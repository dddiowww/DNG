name: dng
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.0
      - torchvision>=0.15
      - einops>=0.7
      - numpy
      - pandas
      - tqdm
      - scipy
      - scikit-learn
      - opencv-python
      - thop
      - vit-pytorch
      - pytest>=7.0
